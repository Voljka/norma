var assert = require('assert');
var should = require('should');

describe('Server', function() {
	
	describe('Start', function() {
		it('Server should been started!');
	})

	describe('Database', function(){
		it('Database exists')

		describe('Users', function(){

			it('User lists')
			it('User saving')
			it('User count')
			it('Should return empty array if user does not exist');
			it('New user must be locked before creating new password')
		})
	})
})

describe('Front-end', function(){
	describe('Checking configs', function() {
		it('Mainmenu should have login, logout, changePassword items')
		it('Mainmenu items should have i18n captions')
	})
})